# Frontend Environment Variables for Railway Deployment
# Copy this file to .env.local for local development

# Google OAuth Configuration (Required for frontend)
VITE_GOOGLE_CLIENT_ID=your_google_client_id_here

# API Configuration
VITE_API_URL=http://localhost:8000
VITE_ENVIRONMENT=development

# Note: VITE_ prefix is required for Vite to expose variables to the frontend
# These variables must be set in Railway dashboard under Variables tab
